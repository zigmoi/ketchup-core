apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ketchup.fullname" . }}-test-connection"
  labels:
{{ include "ketchup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ketchup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
