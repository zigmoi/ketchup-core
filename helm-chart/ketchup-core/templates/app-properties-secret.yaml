apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ketchup-core.fullname" . }}
  labels:
    {{- include "ketchup-core.labels" . | nindent 4 }}
data:
  application.properties: |-
     {{ .Values.applicationProperties | b64enc }}