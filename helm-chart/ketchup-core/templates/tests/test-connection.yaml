apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ketchup-core.fullname" . }}-test-connection"
  labels:
{{ include "ketchup-core.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ketchup-core.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
