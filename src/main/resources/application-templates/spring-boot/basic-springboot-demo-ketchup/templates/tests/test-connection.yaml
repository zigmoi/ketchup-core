apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basic-springboot-demo-ketchup.fullname" . }}-test-connection"
  labels:
{{ include "basic-springboot-demo-ketchup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "basic-springboot-demo-ketchup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
